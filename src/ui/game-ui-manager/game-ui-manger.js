

// This is the implementation of the game UI manager
// Transition from;
//   - player selection
//   - board configuration
//   - player game (from here the init state of player selection is re-executed)

const GameUIManager = () => {

    // Referrence the main div we can use here
    const mainDisp = document.querySelector('');

    const initGameManager = () => {
        // Init the game Manager
        const playerSelectionScreen = undefined;
    }

    return { nextState }
}